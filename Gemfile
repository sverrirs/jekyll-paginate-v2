source 'https://rubygems.org'

# Specify your gem's dependencies in jekyll-paginate-v2.gemspec
gemspec

 # Minitest required for travis bunder run
gem 'minitest', '~> 5.4', '>= 5.4.3'

if ENV["JEKYLL_VERSION"]
  gem "jekyll", "~> #{ENV["JEKYLL_VERSION"]}"
end